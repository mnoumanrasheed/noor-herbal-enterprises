# Copy this file to .env.local for local development. Do not commit .env.local.
# Use real values from your own Neon, Cloudinary, and password manager accounts.

# Neon Console -> Project -> Connection Details -> pooled Postgres connection string
DATABASE_URL=postgresql://neondb_owner:npg_TViu2rtCq5wS@ep-shy-moon-b4lktlml-pooler.c-6.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Generate locally with: openssl rand -base64 32
AUTH_SECRET=replace-with-a-long-random-auth-secret

# The sole administrator allowed to sign in. ADMIN_PASSWORD is used only by db:seed
# to create the first admin and is never sent to the browser.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Noor{}/$herbal6281

# Cloudinary Console -> Dashboard -> Product Environment Credentials
# These remain server-side and are used only by the authenticated upload/delete workflow.
CLOUDINARY_CLOUD_NAME=uwycgpl3
CLOUDINARY_API_KEY=513451764197132
CLOUDINARY_API_SECRET=1DrzGxlUt-Yq2_T9aKRv6Z23JE8

# Public deployment URL. Use http://localhost:3000 locally and the Vercel URL in production.
AUTH_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
